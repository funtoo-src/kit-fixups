tcpdump:
  generator: github-1
  packages:
    - tcpdump:
        github:
          user: the-tcpdump-group
          repo: tcpdump
          query: tags
          transform:
            - kind: string
              match: 'tcpdump-'
              replace: ''
